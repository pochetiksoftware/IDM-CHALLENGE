server:
  port: 8082

spring:
  application:
    name: cliente-service

  datasource:
    url: jdbc:h2:mem:clientesdb
    driverClassName: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true
